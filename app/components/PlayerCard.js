import React, { useState, useEffect } from 'react';
import { View, Text, Image, StyleSheet, TouchableOpacity, Alert, ScrollView, Share } from 'react-native';
import { getAllPlayers, updatePlayer } from './utils/database'
  },
});