import React from 'react';
import { View } from 'react-native';

// Placeholder - react-native-google-mobile-ads requires a custom dev client
export default function AdBanner() {
  return <View />;
}